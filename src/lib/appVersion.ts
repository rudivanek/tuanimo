// Single source of truth for the app version shown in the UI.
// Shown in the desktop header tagline and on the Settings screen (mobile).
// Bump this value when you ship a new release.
export const APP_VERSION = '2.5';